name: pytorch-ddp-multinode-torchrun
tags: [pytorch, ddp, training, multi-gpu, multi-node, torchrun, release]
description: PyTorch multi-node training using Distributed Data Parallel and torchrun

job:
  - srun
  - --nodes=4
  - --gpus-per-node=8
  - --ntasks-per-node=1
  - --cpus-per-task=56
  - --time=00:30:00
  - singularity
  - run
  - "{{ extra_args.sif }}"
  - bash -c
  - "{{ snippet.script }}"

snippets:
  - name: script
    content: |
      # Avoid error mentioned in https://github.com/pytorch/pytorch/issues/60477
      export TMPDIR=$(mktemp -d)
      # Avoid error mention in https://github.com/lumi-ai-factory/laifs-container-recipes/issues/20
      export CUDA_LAUNCH_BLOCKING=1

      # If this is not set, torchrun sets it and prints a warning message
      export OMP_NUM_THREADS=1

      # This would be cleaner with scontrol, but it is not available in the container
      HOST_NODE_FQDN="nid$(echo $SLURM_JOB_NODELIST | grep -Eo [0-9]+ | head -n 1)"
      # Set rendezvous port using job ID, as the default port is likely in use
      HOST_NODE_PORT="1${SLURM_JOB_ID:0-4}"

      source .virtualenvs/$(basename $SINGULARITY_NAME .sif)/bin/activate

      cd benchmarks/pytorch

      python3 -m torch.distributed.run \
          --nnodes=$SLURM_JOB_NUM_NODES \
          --nproc_per_node=$SLURM_GPUS_PER_NODE \
          --rdzv-id=$SLURM_JOB_ID \
          --rdzv-backend=c10d \
          --rdzv-endpoint=$HOST_NODE_FQDN:$HOST_NODE_PORT \
          ddp_visiontransformer.py

parse: |
  def parse(text, params):
      import re
      from statistics import mean

      results = {
          "loss_avg": {
              "pattern": r"Loss: (\d+\.\d+)",
              "values": [],
          },
          "acc_avg": {
              "pattern": r"Accuracy: (\d+\.\d+)",
              "values": [],
          },
          "time_avg": {
              "pattern": r"Time: (\d+\.\d+)",
              "values": [],
          },
      }

      for key, val in results.items():
          results[key]["values"] = [
              float(s) for s in re.findall(val["pattern"], text)
          ]

      return {k: mean(v["values"]) for k, v in results.items()}

validate: |
  def validate(results, params):
      import json

      if not results:
          return (False, "no results to validate")

      limits = {
          "loss_avg": {"type": "max", "unit": "", "value": 5.0},
          "acc_avg": {"type": "min", "unit": "%", "value": 5.0},
          "time_avg": {"type": "max", "unit": "s", "value": 60.0},
      }

      all_ok = True
      validate_res = {}

      for metric, limit in limits.items():
          res = results[metric]
          ok = True

          if (
              limit["type"] == "max" and res > limit["value"]
              or limit["type"] == "min" and res < limit["value"]
          ):
              ok = False
              all_ok = False

          validate_res[metric] = {
              "limit": f"{round(limit['value'], 2)} {limit['unit']}".strip() + f" ({limit['type']})",
              "value": f"{round(res, 2)} {limit['unit']}".strip(),
              "status": "pass" if ok else "fail",
          }

      return (all_ok, "results:\n```\n" + json.dumps(validate_res, indent=4) + "\n```")
